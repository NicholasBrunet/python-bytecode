[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pycompiler"
version = "0.1.1"
description = "Compiler component of pyEngine"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    
]

[tool.hatch.build.targets.wheel]
packages = ["src/pycompiler"]
